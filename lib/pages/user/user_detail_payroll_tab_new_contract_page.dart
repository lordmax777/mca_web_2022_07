import '../../theme/theme.dart';

class UserDetailsPayrollTabNewContractPage extends StatelessWidget {
  const UserDetailsPayrollTabNewContractPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
