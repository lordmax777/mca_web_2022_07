import 'package:flutter/material.dart';

class CompanyTab extends StatelessWidget {
  const CompanyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('Company Tab'),
    );
  }
}
